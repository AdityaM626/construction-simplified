# PostgreSQL Database Connection URL
# Replace credentials with your PostgreSQL instance (e.g. Supabase, Neon, AWS RDS, or local Postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/construction_os?schema=public"

# Express API Port
PORT=4000
